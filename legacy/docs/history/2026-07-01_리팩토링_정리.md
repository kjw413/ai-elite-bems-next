# 2026-07-01 저장소 리팩토링 정리 (1단계)

> 목적: 반복 개발로 누적된 임시/진단/연구 산출물과 중복 문서를 정리하고, 폴더 상하위 구조를
> 명료화한다. 개발 히스토리(한계점 개선 이력)는 보존한다. RPA(데이터 수집·재가공) 분리는
> 2단계로 진행한다.

## 배경

- 5개 공장(남양주1·2·김해·광주·논산) 에너지/생산 데이터를 다루는 Streamlit 웹앱 저장소가
  장기 개발로 진단 스크립트·연구 산출물·중복 문서로 비대해짐.
- 정리는 **격리(quarantine) 방식** — 추적 파일은 삭제 대신 `_cleanup_trash/2026-07-01/`로
  상대경로 보존 이동(가역적). 런타임 필수 파일은 보존.

## 1단계에서 한 일

### 1) 임시 잔재 삭제 (gitignore된 재생성 가능 파일)
- 에디터 atomic-write 임시파일 `*.tmp.*` 162개
- `__pycache__/` 25개, `catboost_info/` 3개

### 2) 진단·연구 산출물 격리 → `_cleanup_trash/2026-07-01/`
- `diagnostics/` 폴더 전체 (ablation·backtest·baseload·gimhae·smoke 스크립트 + txt/png 출력)
- `app/predictive model/` 연구 스크립트: `analyze_gimhae_may_outliers.py`,
  `analyze_gimhae_per_day.py`, `analyze_wip_correlations.py`, `verify_bottling_feature.py`,
  `wip_feature_shortlist.py`
- `app/predictive model/energy usage/` 연구 CSV 5종 + 성능표 xlsx 5종
- `app/predictive model/wip_analysis/` 폴더 전체 (재공품 상관분석 산출물)
- `analysis_results/`의 분석 중간 CSV 3종 (`category_energy_intensity.csv`,
  `item_energy_correlations.csv`, `item_energy_intensity_top.csv`)
- `app/predictive model/공장별_품목별_상관분석.xlsx`, `에너지예측_품목추천.xlsx`
- 루트 예측 export CSV 2종

> **보존한 런타임 필수 파일**: `app/predictive model/energy usage/`의 `*.pkl`,
> `v5_model_registry.json`, `v5.3_*_perf.json` (활성 모델), `DB_holiday.xlsx`,
> `DB_weather.xlsx`, `analysis_results/item_energy_impact_lookup.json`
> (`item_energy_impact_service`가 런타임 로드), `analysis_results/item_energy_impact_report.md`.
> `app/predictive model/_archive/`(구버전 모델링 이력)와 `modeling_v5.1/v5.3.py`(현행 학습기)도 유지.

### 3) 문서 통합·재구성
- **명확한 중복 제거**: `docs/BEMS_기능설명_임원용.md`(198줄) 격리 — 확장·최신판
  `docs/기능설명.md`(406줄)로 대체됨.
- **`docs/history/` 신설** — 개발 이력/계획 문서 집결 (#5 이력 보존):
  `시스템_개선_제언_2026-06-12.md`, `생산실적_분석_페이지_개선안_2026-06-26.md`,
  `RPA_개발기록_MIS_자동화.md`, `260423_집계방식개선안.md`, `HF_TiDB_migration_plan.md`,
  `개선필요사항.xlsx`, `PLAN_AI_예측모델_재학습_완전통합.md`(루트에서), `predictive_model_PLAN.md`,
  `gemini_pro_제언사항.md`, 그리고 본 문서.
- **코드 폴더의 설명 문서 → `docs/`**: `머신러닝_모델링_상세명세.md`, `머신러닝_모델링_안내.md`
- 루트 `project_FEMS_info.md` → `docs/`
- `docs/notes/` 폐지(내용은 `docs/history/`로 이동)

### 4) 폴더/이름 정리
- `app/API prompts/` → `app/prompts/` (공백 포함 폴더명 제거; 코드가 이미 `prompts/` 우선 탐색)
  - `ai_report_service._resolve_report_prompt_path`, `anomaly_diagnosis_service._resolve_prompt_path`의
    죽은 `API prompts` 폴백 경로 제거

## 정리 후 구조 (요지)

```
├── app/
│   ├── prompts/                 # (구 "API prompts")
│   ├── predictive model/        # 런타임 모델 + 현행 학습기 + _archive(이력)
│   │   ├── energy usage/        # *.pkl · v5_model_registry.json (런타임)
│   │   ├── modeling_v5.1.py / modeling_v5.3.py
│   │   └── _archive/
│   ├── config/ database/ domain/ pages/ services/ utils/ assets/
├── docs/                        # 모든 문서
│   ├── history/                 # 개발/개선 이력 (#5)
│   ├── diagrams/ img/ references/
│   └── (스펙·DB·모델·UI 설명서)
├── tools/                       # git_hooks / mail / scripts / mis_rpa / utils
├── tests/  data/(gitignore)
└── RUN_GUIDE_KR.md              # 루트 실행 가이드
```

## 격리 폴더 안내

- 격리물은 `_cleanup_trash/2026-07-01/`에 상대경로 그대로 보존됨(가역적, gitignore).
- 검토 후 불필요하면 폴더째 삭제, 되살릴 항목은 원위치로 이동하면 됨.
- 참고: 직전 정리 격리물은 `_cleanup_trash/2026-06-12/`.

## 2단계 예정 — RPA(데이터 수집·재가공) 분리

**목표 아키텍처**: RPA는 MIS 화면 → 엑셀 수집 + **형식 재가공**까지 담당하는 독립 프로젝트로
분리하고, **웹앱 DB 적재는 서버 기동 시**(이미 구현된 `auto_sync_once`/`auto_sync_production_once`)
수행. 두 프로젝트의 접점은 `E:\Sampled DB` 폴더의 엑셀 파일뿐.

- 분리 대상 → 형제 폴더 `../AI-Elite_MIS_RPA/`: `tools/mis_rpa/`, `tools/utils/`
- 재가공 로직 이관: `production_dw_service`의 build 반쪽(`build_dataset` 등, DB 무관 순수 파일 I/O)과
  `tools/scripts/WIP_refactoring.py`를 RPA 프로젝트로 이동. 웹 `production_dw_service`는
  쿼리 함수(`query_production_daily`/`query_monthly_summary`/`query_distinct_items`)만 유지.
- 경로 헬퍼 `app.config.paths.sampled_db_path_str`는 RPA 측에 소형 복제.
- 잔류: `tools/mail/`, `tools/scripts/`(마이그레이션), `tools/git_hooks/`.
- 에너지 파이프라인은 이미 이상적 형태(RPA→`RawDB_에너지.xlsx`, 웹 startup이 raw 직접 적재).
- 코드 리팩터이므로 별도 단계에서 테스트와 함께 진행.

## 2단계 완료 — RPA 분리 (2026-07-01)

형제 프로젝트 `../AI-Elite_MIS_RPA/` 신설(독립 git 저장소). 웹앱과의 접점은 `SAMPLED_DB_DIR` 엑셀 파일뿐.

**RPA 프로젝트 구성:**
- `mis_rpa/`: 수집 RPA(`production/utility/wip_daily_rpa.py`, `_common.py`, `run_all_rpa.py`, `build_production_dataset.py`, `*_coords.json`, `*.bat`)
- `mis_rpa/production_builder.py`: 웹 `production_dw_service`의 **build 반쪽(재가공)** 이관(순수 파일 I/O, ~1360줄). `build_dataset`/`parse_sheet`/`consolidate_*`/`save_db_file` 등.
- `mis_rpa/wip_refactoring.py`: `tools/scripts/WIP_refactoring.py` 이관.
- `mis_rpa/config.py`, `mis_rpa/factories.py`: 웹 `app/config/paths.py`·`app/domain/factories.py` 복제(경로/공장코드 상수).
- `utils/`: 좌표 측정·클릭 기록 도우미.
- `tests/test_production_builder.py`: 웹 `tests/test_production_dw_new_structure.py` 이관(임시 fixture로 `build_dataset` E2E 검증 — **통과 확인**).
- `README.md`/`requirements.txt`/`.env.template`/`.gitignore`.

**웹앱 변경:**
- `app/services/production_dw_service.py`를 **조회 함수만**(`query_production_daily`/`query_monthly_summary`/`query_distinct_items` + `DEFAULT_OUTPUT_PATH`)으로 슬림화(1521→174줄). build 반쪽은 RPA로 이관.
- 제거: `tools/mis_rpa/`, `tools/utils/`, `tools/scripts/WIP_refactoring.py`(→ RPA).
- 격리: `tools/scripts/migrate_f10_legacy.py`, `migrate_production_to_new_structure.py`(제거된 `build_dataset` 의존 일회성 마이그레이션).
- DB 적재는 기존대로 서버 기동 시(`auto_sync_once`/`auto_sync_production_once`) 수행 — 변경 없음.

**검증:** RPA 모듈 전체 standalone import(app.* 의존 0) + build_dataset E2E 테스트 통과 + 웹 app/ 전체 컴파일 + 슬림 서비스/동기화 서비스 import 정상.

**후속(사용자 조치 필요):**
- `production_performance.py`의 안내 메시지 2곳이 아직 `tools/mis_rpa/build_production_dataset.py`를 가리킴 → RPA 프로젝트 경로로 갱신 필요(이 파일은 사용자 WIP라 미수정).
- RPA 실제 구동(MIS 화면 자동화)은 사용자 PC에서 검증 필요. 설치: `AI-Elite_MIS_RPA/README.md` 참고(자체 venv + `.env`의 `SAMPLED_DB_DIR`을 웹앱과 동일하게).

## 3단계 완료 — 문서 통합·현행화 (2026-07-01)

1단계에서 "명확한 중복만" 제거했으나, 스펙 계열이 여전히 겹치고 대부분 "FEMS 초기단계"로 낡아 전면 통합·현행화 수행.

**스펙/개요 6개 → 2개로 통합:**
- `MASTER_SPEC_KR.md` — 정본 시스템 사양서로 전면 개정. 현행 반영: BEMS, 11 페이지(생산실적·웹예측 구현됨), 24 서비스(도메인별), 9 테이블, v5.3 분위수 예측, IP 인증, 기동 시 자동 적재, **RPA 분리**, `app/prompts/`.
- `AGENT_GUIDE_KR.md` — AI 작업 가이드 현행화(IP 인증, 구현 상태, 현행 파일/서비스 맵, RPA 분리, 훅).
- 격리 → `_cleanup_trash/2026-07-01/docs/`: `functional_specification.md`, `poc_spec_v1_1.md`(MASTER 부분집합), `ai_dev_package.md`(영문 요약), `project_FEMS_info.md`.

**DB 계열:** `DB_DIAGRAM_KR.md`(9테이블 ERD) 정본 + `Data_dictionery_v1.0.md`(업로드 사전) 유지 → `energy_db_schema_KR.md`(4테이블 낡은 부분집합) 격리.

**유지 문서 현행화(타깃 수정):**
- `기능설명.md`: 예측 버전 v5.1/v5.2 → **v5.3**.
- `energy_ui_wireframe_KR.md`: FEMS → BEMS.
- `머신러닝_모델링_상세명세.md`·`머신러닝_모델링_안내.md`: v5.2 "적용 예정" → v5.2/v5.3 **적용 완료(운영 중)**.
- `RUN_GUIDE_KR.md`(루트): FEMS→BEMS, "비밀번호 로그인" → IP 자동 판정, `tools/mis_rpa` 안내 → RPA 프로젝트 분리 반영.

**결과:** `docs/`는 스펙/DB/모델/UI/이력으로 정리된 11개 md. 공통 낡음(생산·예측 미구현 표기, 4~5테이블, 비밀번호 로그인, RPA 미반영) 모두 해소.
