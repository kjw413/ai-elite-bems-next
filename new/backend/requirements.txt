# FastAPI 브리지 전용 추가 의존성.
# SETUP_LOCAL.bat가 legacy requirements와 함께 독립 new/.venv에 설치합니다.
# app.services.* 가 streamlit/pandas/lightgbm 등 기존 requirements 전체를 요구합니다.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pymysql>=1.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
