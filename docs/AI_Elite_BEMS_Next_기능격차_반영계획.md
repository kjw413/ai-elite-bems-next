# AI Elite BEMS Next — legacy 대비 기능 격차 분석 및 반영 계획

> 작성일: 2026-07-17 · 기준: 로컬 git 이력의 legacy 최종 스냅샷(`e1fef60~1`, "legacy로부터 완전 독립화 완완료" 직전 커밋) vs 현재 `main` 작업트리
>
> **비교 출처 참고**: 비교 기준은 이 저장소 git 이력 안의 `legacy/` 폴더 최종본이다
> (스냅샷 추출: `git archive e1fef60~1 legacy`). 분석 당시 GitHub
> `kjw413/ai-elite-bems-legacy`는 private라 MCP 조회가 404였으나, 2026-07-17
> public 전환 후 클론·전체 diff로 대조한 결과 **로컬 스냅샷과 167개 파일 전부
> 동일**함을 확인했다(차이 0건, 커밋 `6fa4da7` "bems-next로부터 완전 독립화 완료").
> 따라서 본 격차표는 GitHub legacy 저장소 기준으로도 그대로 유효하다.

---

## 1. 격차 전수 목록 (화면·섹션 단위)

표기: ✅ 동등 구현 · 🔶 부분 구현 · ❌ 미구현 · ➖ 의도적 제외

### 1.1 대시보드 (legacy `app/pages/dashboard_main.py`)

| legacy 섹션 | 현재 상태 | 격차 내용 |
|---|---|---|
| KPI 요약 (원단위 3종 + 누계 생산량, 전년비) | ✅ | — |
| KPI 카드의 목표 대비 달성(목표 설정 모달 연동) | 🔶 | 목표는 관리자 화면에서 관리하나 대시보드 KPI에 목표 대비 표시는 없음 |
| AI 이상감지 알림 배너 (7일 이탈 건수 + LLM 분석 버튼) | 🔶 | 건수 배너는 있음. 상세 분석은 AI 예측 화면의 진단 기능으로 대체 |
| **7일간 생산량·사용량 방향 비교** (전력/연료/용수/폐수 4분면, 지수=7일 중앙값 100, 생산↑사용↓/생산↓사용↑ 신호, 상세 테이블, CSV) | ❌→✅ | **P0-C 이번 구현.** 기존에는 "최근 7일 전력 사용량 vs AI 예측"만 존재 |
| 월간 전년비 (원단위/사용량/생산량 3모드 + 상세 비교 테이블) | ❌→✅ | **P1 반영(2026-07-17).** 공장별 4분면 막대 3모드 + 상세 테이블·CSV |
| 월간 주요 이슈(🔔 자동 이슈 알림 리스트) | ❌→✅ | **P1 반영(2026-07-17).** legacy 규칙 동일(Top 5·심각도) |
| 에너지 구성 도넛 | ❌→✅ | **P2 반영(2026-07-18).** YTD 공장별 비중 도넛 4종(공장 아이덴티티 색) |
| 임원 요약 One-Page 인쇄 뷰 | ➖ | 사용자 결정(2026-07-17)으로 이식 제외 |
| **📧 메일 송부 (관리자, 일간/주간/월간 즉시 발송)** | ❌→✅ | **P0-A 이번 구현.** tools/mail 빌더·발송기는 이관 완료였으나 UI/API 트리거가 없었음 |
| 최근 이벤트 메모 | ✅ | — |

### 1.2 에너지 사용량 (legacy `app/pages/energy_usage.py` + `app/components/energy_usage.py`)

| legacy 섹션 | 현재 상태 | 격차 내용 |
|---|---|---|
| 에너지원 선택 (전력/연료/용수/폐수) | ✅ | — |
| 사용량 추이 — 월 단위/**기간 지정** 토글, 공장별 비교 | ❌→✅ | **P0-B 기간 지정 + P1 설비 분해 + P2 공장별 비교 라인(전사 조회 시 토글) 완료** |
| KPI (총량·일평균·최대) | ✅ | 전력 분해 KPI(냉동/공압/기타)는 P1 |
| **전년대비 사용량 분석 (금년 vs 전년 월별 차트 + 월별·누계 테이블)** | ❌→✅ | **P0-B 이번 구현.** "기간, 연간 누계 실적 조회" 격차의 본체 |
| 공장별 폐수/용수 비율 (막대+테이블) | ❌→✅ | **P1 반영(2026-07-17).** 용수·폐수 선택 시 비율 막대+CSV |

### 1.3 에너지 원단위 (legacy `app/pages/energy_intensity.py`)

| legacy 섹션 | 현재 상태 | 격차 내용 |
|---|---|---|
| MTD/YTD 요약, 연간 월별 추이(전년/목표/금년), 공장 매트릭스 | ✅ | — |
| 일별/기간별 원단위 추이 (공장 선택, 누계 토글) | ❌→✅ | **P1 당월/기간 지정 + P2 누계 추이 토글(Σ사용량÷Σ생산톤) 완료** |
| 전년대비 원단위 월별 테이블 + 누계 행(가중평균 Σ사용량÷Σ생산톤) | ❌→✅ | **P1 반영(2026-07-17).** 가중 동월 누계 서버 계산 |

### 1.4 생산실적 분석 (legacy `app/pages/production_performance.py`)

| legacy 섹션 | 현재 상태 | 격차 내용 |
|---|---|---|
| 월별/기간별/연간 모드, KPI, 믹스, 일별 추이, Top 품목, Burn-up | ✅ | Phase 4 완료분 |
| 에너지 vs 생산량 중첩 차트 | ❌ | P1 |
| 연간 월별 실적 전년비 (구 월별 누계 추이) | ❌→✅ | **P2 반영(2026-07-18).** 연간 모드에 월별 생산량 전년비 막대 |
| 계획 미달/초과 Top 탭 분리 | ❌→✅ | **P2 반영(2026-07-18).** 실적/미달/초과 Top 탭 + 편차 컬럼 |
| 자동 인사이트 문장(_generate_insights) | ❌→✅ | **P2 반영(2026-07-18).** 진척 구간·최대/부진 제품유형 규칙 동일 |
| What-if 시뮬레이터 | ➖ | legacy가 2026-06-26 UI 제거 확정 — 이식 제외(기존 결정 유지) |

### 1.5 AI 예측 (legacy `app/pages/ai_prediction.py`)

| legacy 섹션 | 현재 상태 | 격차 내용 |
|---|---|---|
| 예측 실행(공장·기간), 예측 이력, 재학습, 기상 동기화 | ✅ | — |
| 입력 데이터 편집(생산량·기온 수정, 엑셀 복붙) | 🔶 | 단일 예측 미리보기의 생산계획 kg 입력만 지원 → P1 |
| 모델 변수 영향도 Top 5 (한국어 설명) | ❌→✅ | **P1 반영(2026-07-17).** feature-importance API + 예측 화면 카드 |
| 예측 모니터링 패널(offset 감지) | ❌→✅ | **P2 반영(2026-07-18).** monitoring API + 모델 성능 감지 패널 |
| 이상감지 현황 탭(`anomaly_monitor` 1,287줄) | 🔶 | 진단 API·마크다운 패널 + 모니터링 패널로 축약 구현 (전체 탭 1:1 이식은 범위 외) |

### 1.6 도구·자동화 (legacy `tools/`)

| 항목 | 현재 상태 | 격차 내용 |
|---|---|---|
| tools/mail 빌더·발송기·템플릿·스케줄 bat | ✅ | 2026-07-17 독립화 완료 (diff 검증: bat 2종의 의도적 .venv 경로 수정 외 동일) |
| **웹 UI에서 메일 즉시 발송** | ❌→✅ | **P0-A 이번 구현** |
| tools/scripts (영향계수 분석 등) | ✅ | 산출물(lookup json) 이관 완료, 원본 스크립트는 legacy 이력에 보존 |

---

## 2. 이번 반영 범위 (P0 — 사용자 지목 3건)

### P0-A. 메일 리포트 발송 UI/API ("메일 alert")
- `POST /api/v1/mail/send` (관리자 전용): `{period: daily|weekly|monthly, date?}`
  - `tools.mail` 빌더/발송 파이프라인 재사용 (legacy `_send_dashboard_report_mail` 동일 규칙)
  - 일간=기준일(미지정 시 빌더의 근무일 D-1 규칙), 주간=직전 완결 주, 월간=직전 완결 월
  - 설정 누락 시 누락 키 목록과 함께 503 반환, 발송 실패는 502
- 대시보드 상단(관리자에게만 노출): 주기 선택 + 발송 버튼 + 결과 표시

### P0-B. 에너지 사용량 — 기간 지정 조회 + 전년대비(연간 누계) 분석
- `GET /api/v1/energy`에 `date_from`/`date_to` 추가 (미지정 시 기존 30일 유지)
- 응답에 `yoy` 블록 추가: 기준연도 1~12월 금년/전년 사용량(4개 에너지원 전부)
- 화면: 조회 방식 토글(최근 30일/기간 지정) + "전년대비 사용량" 섹션
  - 금년 vs 전년 월별 막대 차트 (선택 에너지원)
  - 월별 테이블: 전년 실적·금년 실적·증감량·증감률 + **누계 행** (CSV 지원)

### P0-C. 대시보드 — 7일간 생산량·사용량 방향 비교
- `GET /api/v1/dashboard`의 `trend`에 연료·용수·폐수 일별 사용량 추가
- 화면: 전력/연료/용수/폐수 4분면 차트
  - 생산량 지수 vs 사용량 지수 (7일 중앙값=100, legacy `_median_index` 규칙)
  - 방향 신호: 생산↑사용↓(파랑)·생산↓사용↑(빨강) 마커, 중앙값 1% 이하 변동 무시
  - 상세 테이블 토글 + CSV (legacy 컬럼 구성 동일)

## 3. 후속 우선순위 및 P1 반영 결과 (2026-07-17 갱신)

- **P1 (반영 완료)**:
  - [x] 대시보드 월간 전년비 3모드(원단위/사용량/생산량, 공장별 4분면 막대)+상세 비교 테이블
    — `/dashboard`의 `yoyFactories` 블록 + `components/factory-yoy.tsx`, 색 규칙 legacy 동일
    (원단위 +빨강/−파랑, 생산량 반전, 사용량 중립)
  - [x] 월간 주요 이슈 — legacy `_render_issue_alerts` 규칙(원단위 악화 전건·생산량 10%↑ 감소,
    심각도 Top 5, 🔥 5%↑) 클라이언트 계산
  - [x] 에너지 전력 설비 분해 — 일별 추이에 전체/냉동/공압/기타 라인(`freezing/compressor/other`)
  - [x] 공장별 폐수/용수 비율 — 용수·폐수 선택 시 비율 막대+CSV
  - [x] 원단위 일별·기간별 추이(`date_from/date_to`) + 전년대비 월별 테이블·**가중 누계 행**
    (Σ사용량÷Σ생산톤, 동월 누계 — `weighted_intensity_yoy`)
  - [x] 모델 변수 영향도 — `GET /api/v1/model/feature-importance`(v5_explainability 위임,
    Top 5 한국어 라벨+요약 문장) + 예측 화면 카드
  - [ ] 예측 입력 데이터 편집(생산량·기온 수정, 엑셀 복붙 그리드) — 규모가 커서 잔여 P1로 유지
- **제외(사용자 결정 2026-07-17)**: ~~임원 요약 One-Page 인쇄 뷰~~ — 구현하지 않기로 확정
- **P2 (2026-07-18 반영 완료)**:
  - [x] 대시보드 에너지 구성 도넛 — `/dashboard` `composition`(YTD) + 도넛 4종, 공장 아이덴티티 색
    (legacy DASHBOARD_FACTORY_COLORS; 연접 페어 CVD WARN 6.2는 슬라이스 % 라벨·범례·툴팁으로 relief)
  - [x] 에너지 공장별 비교 라인 — `dailyByFactory`(전사 조회) + '공장별 비교' 토글
  - [x] 원단위 일자 누계 토글 — 일별 응답에 usage·productionTon 추가, 클라이언트 누계 재계산
  - [x] 생산: 연간 월별 전년비(`monthlyYoy`) / 미달·초과 Top 탭(`underItems`·`overItems`) /
    자동 인사이트(`build_production_insights` — legacy _generate_insights 규칙)
  - [x] 예측 모델 성능 감지 — `GET /api/v1/predictions/monitoring`(prediction_monitoring_service 위임)
    + overall 배너·KPI·상세 테이블 패널
- **잔여 없음** — P0·P1·P2 전 항목 반영 완료 (이식 제외 확정: What-if, 임원 요약 인쇄 뷰,
  예측 입력 데이터 편집 그리드)

## 5. 세부 개선 (2026-07-18 사용자 피드백) — 페이지·섹션 단위

legacy 격차와 별개로, 사용성·레이아웃·시각 규칙에 대한 사용자 피드백을 반영했다.

### 공통
- [x] 모든 꺾은선 차트를 곡선(monotone) → **직선(linear)** 으로 통일 (11개 `type` + 기본값 라인)
- [x] 에너지 사용량·원단위 페이지에서 **차트는 상시 노출**하고 그 아래 **상세 데이터 표만
  접기 토글(`<details>`, 기본 접힘)** — `DataToggle` 헬퍼. (당초 카드 전체를 접었으나
  2026-07-18 사용자 피드백: "차트는 상시, 그 밑 데이터만 토글"로 정정)

### 통합 대시보드
- [x] 생산량 상징색 연노랑 → **연초록 `#7bc96f`** (dataviz 검증기로 에너지원 색과 CVD 분리 확인)
- [x] 월간 주요 이슈(좁게)와 공장별 에너지 비율 도넛(넓게)을 **한 행(`.dash-pair` 1fr:2fr)** 에 배치

### 에너지 사용량
- [x] 전년대비 사용량 섹션을 막대 → **꺾은선**(금년=에너지원 상징색, 전년=회색)
- [x] 전년대비 사용량(넓게)과 공장별 사용량(좁게)을 나란히 배치
- [x] 용수·폐수 서브페이지: 전년대비(2fr, 크게) + 공장별 폐수/용수(1fr, 작게) 나란히

### 에너지 원단위
- [x] 차트 색을 에너지원 상징색으로 (전력 주황·연료 빨강·용수 하늘)
- [x] 누계 추이 보기 토글을 일별 차트 → **연간(2026년) 원단위 추이 카드**로 이동
  (legacy 위치와 동일, 각 월을 1월부터 가중 누계로 재계산) — 월별 응답에 usage/ton 원자료 추가
- [x] 공장 효율 매트릭스와 연간 원단위 추이를 나란히 배치(각 가로폭 축소)

### 생산실적 분석
- [x] "예상 착지" → **"월말/연말 예상 실적"** 로 어휘 변경
- [x] 제품유형별 일일 생산량을 가로 전체(`span-all`)로 확대, 주요 품목(넓게)+제품 믹스(좁게) 나란히
- [x] **신규 섹션 — 품목 실적 추이·비교**: 단일 품목의 월별 추이(실적+전년 동월, 전월비·전년동월비
  배지) 또는 두 품목 월별 실적 비교. `GET /production/items`(최근 12개월 상위 100품목),
  `GET /production/item-trend`(최근 13개월, 2년 조회로 전년 동월 부착) 신설

### 관리자·현장 메모
- [x] 메일 송부를 데이터·동기화 탭에서 분리해 **'메일 리포트' 전용 탭** 신설 — 관리자만 노출,
  발송 전 실제 HTML 본문을 iframe으로 미리보기(`GET /mail/preview`, cid 이미지 → data URI)

### 세부 개선 검증 결과 (2026-07-18)
- 백엔드 테스트 50건(신규 3건: shift_month·품목 코드 검증·메일 preview data URI) 통과,
  typecheck·프로덕션 빌드 통과
- headless Edge(CDP) 라이트/다크: 대시보드 이슈+도넛 한 행·전년비 4분면 상징색, 에너지 접힘(2×2)·
  전년대비 꺾은선·공장별 나란히·폐수 배치, 원단위 상징색·연간 누계 토글·매트릭스 나란히,
  생산 레이아웃·품목 추이 섹션, 관리자 메일 미리보기 탭 — 콘솔 오류 0

### P1 검증 결과

- 백엔드 테스트 45건(신규 3건: 가중 누계·공장 전년비 블록·영향도 검증) 통과,
  typecheck·프로덕션 빌드 통과
- headless Edge(CDP): 대시보드 전년비 4분면(3모드 전환)·주요 이슈 4건, 에너지 설비 분해·
  폐수/용수 비율, 원단위 일별 추이·가중 누계 테이블(전체 폭 `span-all` 레이아웃 정비),
  예측 변수 영향도 카드 라이트/다크 렌더링 — 콘솔 오류 0
- 신규 차트 색 조합(전력 분해 blue+amber+green) dataviz 검증기 라이트/다크 통과,
  전년비 막대는 시스템 확립 의미(전년=gray)와 지표별 금년 색 재사용

## 4. 검증 기준 및 결과 (2026-07-17 완료)

- [x] 백엔드 단위 테스트 42건(신규 7건: 에너지 구간·YoY 빌더·메일 period/권한/설정누락·대시보드 trend 확장) 통과
- [x] `npm run typecheck` · `npm run build` 통과 (First Load JS 251kB)
- [x] headless Edge(CDP) 실렌더링: 대시보드(메일 패널·7일 4분면·신호 마커)와
  에너지(기간 지정 모드·전년대비 차트·누계 테이블 13행) 라이트/다크 캡처, 콘솔 오류 0
- [x] 신규 차트 색상 dataviz 검증기 페어 검증 통과 — 폐수 `--chart-wastewater`
  라이트 #a21caf / 다크 #c026d3 신설 (기존 gray는 chroma floor 미달로 탈락)
- 메일 실발송은 실SMTP 없이 설정 누락 503 경로와 CLI(run_mail.bat) 파이프라인 동일성으로 검증,
  실발송은 사내 서버에서 dry-run 후 확인 (기존 운영 체크리스트에 이미 포함)

### 구현 노트 (legacy 대비 의도적 차이)

- 전년대비 누계 행: legacy는 전년 12개월 전체 vs 금년 일부를 합산해 증감률이 왜곡됨
  (예: 7월 기준 -44%) → **동월 누계(1~마지막 실적월)** 로 개선, 행 라벨에 기간 명시
- 메일 발송 버튼 위치: legacy는 대시보드 헤더 popover → 관리자·현장 메모 > 데이터·동기화
  탭의 발송 카드로 이동(2026-07-18 사용자 지시 — viewer 완전 미노출, 발송 전 확인창)
- 상징색(2026-07-18 사용자 지정): 생산 연노랑 #a4d65e(legacy 메일 리포트 동기),
  전력 주황 #f97316(지정 범위 '진한 노랑~주황' 안에서 라임과 CVD 분리 확보), 연료 빨강,
  용수 하늘색, 폐수 회색 — 7일 비교·전년비·에너지 추이/전년대비·비율 차트 일괄 적용
- 월별 조회 하루 누락 수정(2026-07-18): 생산 월별 daily[-14:] 절단 제거(월초 누락),
  에너지·원단위 기본 창을 기준일 역산 30일 → **당월 1일~기준일**로 변경
  (31일 달 완결 조회 시 1일 누락 해소)

### P2 검증 결과 (2026-07-18)

- 백엔드 테스트 47건(신규: 인사이트 규칙·31일 회귀·당월 창) 통과, typecheck·빌드 통과
- headless Edge(CDP): 도넛 4종·공장별 비교 라인·원단위 누계 토글·생산 인사이트 2건·
  미달/초과 탭·모델 성능 감지 패널(DB 미가용 시 오류 메시지 경로 포함) 렌더링,
  콘솔 오류 0
